package com.example.acttrabalho;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class ThirdScreenActivity extends AppCompatActivity {
    /**
     * A classe ThirdScreenActivity representa a terceira tela da aplicação.
     *
     * Esta atividade pode ser usada para exibir informações adicionais sobre o usuário.
     */
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_third_screen);
    }
}